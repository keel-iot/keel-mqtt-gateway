.cache/
__pycache__/
# Generated by run.sh on every run — a CI artifact, not a source file.
# The hand-authored investigation record for a known result lives in
# evidence/ instead (committed) — that doesn't go stale between runs the
# way a raw pass/fail count does.
artifacts/
